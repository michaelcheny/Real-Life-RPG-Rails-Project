<% if object.errors.any? %>
  <div class="card text-white bg-danger mb-3">

    <div class="card-body">

      <h4><%= pluralize(object.errors.count, "error") %> prevented this record from being saved:</h4>

      <ul>
        <% object.errors.full_messages.each do |msg| %>

          <li><%= msg %></li>
          
        <% end %>
      </ul>

    </div>
  </div>
<% end %>