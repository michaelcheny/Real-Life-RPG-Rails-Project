<%= form_with model: task, url: user_task_path(task.user, task), local: true do |f| %>
  <%= f.hidden_field :completed, value: true %>
  <%= f.submit "Complete Task", class: "btn btn-secondary btn-sm btn-block" %>
<% end %>
