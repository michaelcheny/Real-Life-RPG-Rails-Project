<fieldset>
<legend><h3>Overall Highscores</h3></legend>

  <table>
    
    <tr>
      <th>Rank</th>
      <th>Name</th>
      <th>Total Level</th>
      <th>Total Exp</th>
    </tr>
      
    <% sort_by_total_exp(@users).each.with_index do |user, index| %>

      <tr>
        <td><%= index += 1 %></td>
        <td><%= link_to user.username, user_path(user) %></td>
        <td><%= user.total_level %></td>
        <td><%= user.total_exp_pts %></td>
      </tr>

    <% end %>
      
  </table>

</fieldset>